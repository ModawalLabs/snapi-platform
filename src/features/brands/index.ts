/**
 * Public surface of the `brands` slice.
 *
 * Other code imports from here, never from `./components/*` directly — see
 * `src/features/README.md`.
 */
export { BrandIndex } from "./components/brand-index";
export { parseLetterParam } from "./lib/group";
