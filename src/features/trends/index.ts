/**
 * Public surface of the `trends` slice.
 *
 * Other code imports from here, never from `./components/*` directly — see
 * `src/features/README.md`.
 */
export { TrendsBoard } from "./components/trends-board";
