/**
 * Public surface of the `missions` slice.
 *
 * Other code imports from here, never from `./components/*` directly — see
 * `src/features/README.md`.
 */
export { MissionsBoard } from "./components/missions-board";
