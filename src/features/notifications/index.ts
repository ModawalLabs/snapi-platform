/**
 * Public surface of the `notifications` slice.
 *
 * Other code imports from here, never from `./components/*` directly — see
 * `src/features/README.md`.
 */
export { Notifications } from "./components/notifications";
